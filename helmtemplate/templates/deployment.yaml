apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "helmbasics.resourceName" . }}-deployment
  labels:
{{- include "helmtemplate.labels" . | lower }}
spec:
  replicas: {{ .Values.deployments.replicaCount }}
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
{{- include "helmtemplate.labels" . | indent 4 | lower }}
    spec:
      containers:
      - name: nginx
        image: {{ .Values.deployments.image }}
        ports:
        - containerPort: 80
